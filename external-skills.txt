# Skills I want installed but do not maintain here.
#
# Nothing is copied. install.sh symlinks each entry into the harnesses named in the
# third column, so the upstream repo stays the source of truth and `git pull` there
# keeps them current. If a source repo is not cloned on this machine, install.sh
# warns and skips the entry rather than creating a broken link.
#
# Format:  <name>  <path>  [targets]
#   name     installed name — this is what /<name> becomes; need not match the directory
#   path     ~ expands to $HOME
#   targets  both (default) | claude | opencode | claude,opencode
#
# The manifest is authoritative: narrow an entry's targets and the next install.sh
# run prunes its link from the harness it no longer names.

# ---- affaan-m/ECC (MIT) — cloned at ~/src/ECC ----

# Research skills, wanted in both harnesses.
market-research    ~/src/ECC/skills/market-research    both
deep-research      ~/src/ECC/skills/deep-research      both   # needs firecrawl + exa MCPs; inert without them

# Engineering skills previously listed in opencode.json's skills.paths, moved here
# 2026-08-18 so this repo is the single place that decides what is installed.
# Kept opencode-only because that is where they already were — widen to `both` per
# line if you want them in Claude Code too. Note `security-review` would collide
# with Claude Code's bundled skill of the same name.
tdd-workflow       ~/src/ECC/skills/tdd-workflow       opencode
security-review    ~/src/ECC/skills/security-review    opencode
coding-standards   ~/src/ECC/skills/coding-standards   opencode
frontend-patterns  ~/src/ECC/skills/frontend-patterns  opencode
frontend-slides    ~/src/ECC/skills/frontend-slides    opencode
backend-patterns   ~/src/ECC/skills/backend-patterns   opencode
e2e-testing        ~/src/ECC/skills/e2e-testing        opencode
verification-loop  ~/src/ECC/skills/verification-loop  opencode
api-design         ~/src/ECC/skills/api-design         opencode
strategic-compact  ~/src/ECC/skills/strategic-compact  opencode
eval-harness       ~/src/ECC/skills/eval-harness       opencode
